using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeluxeTarget : Target
{
    // Start is called before the first frame update
    void Start()
    {
        pointValue = 15;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
